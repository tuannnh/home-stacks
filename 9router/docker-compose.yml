name: 9router
services:
  9router:
    image: decolua/9router:${NINEROUTER_VERSION:-latest}
    container_name: 9router
    env_file:
      - stack.env
    volumes:
      - /DATA/AppData/9router/data:/app/data
    ports:
      - "11154:20128"
    restart: unless-stopped
    network_mode: bridge
